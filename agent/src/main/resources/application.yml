server:
  port: 9995
  servlet:
    encoding:
      charset: UTF-8
      force: true
      enabled: true
spring:
  main:
    web-application-type: reactive
  ai:
    anthropic:
      base-url: #your base-url
      api-key: @dashscope.api.key@
      chat:
        options:
          model: code-generation-latest
          temperature: 0.7
          max-tokens: 4096
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.1.88:3306/spring_ai_db?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: root
logging:
  level:
    org:
      springframework:
        ai: TRACE
    com:
      alibaba: TRACE
      dahuatech:
        wzagent:
          agent: TRACE